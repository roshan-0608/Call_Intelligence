{
  "name": "@call-intel/frontend",
  "version": "2.0.0",
  "description": "Manager dashboard for reviewing analyzed sales calls, coaching scores and telecaller performance.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit -p tsconfig.json && vite build",
    "preview": "vite preview",
    "//": "tsc -p, not tsc -b: build mode cannot disable emit while referencing the composite core project, which must emit its declarations.",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@call-intel/shared": "*",
    "@tanstack/react-query": "^5.62.7",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "lucide-react": "^0.468.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router-dom": "^7.1.1",
    "recharts": "^3.10.1",
    "tailwind-merge": "^2.5.5"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "@types/react": "^19.0.2",
    "@types/react-dom": "^19.0.2",
    "@vitejs/plugin-react": "^4.3.4",
    "tailwindcss": "^4.0.0",
    "vite": "^6.0.5"
  }
}
